import { createTheme } from "@mui/material/styles";
import { themeHelper } from "../../constants";

export const orangeTheme = createTheme(
  themeHelper(["#2c121b", "#f26f14", "#fbd3b7", "#eee7df"])
);
export const tealTheme = createTheme(
  themeHelper(["#04364a", "#176b87", "#64ccc5", "#dafffb"])
);
export const goldTheme = createTheme(
  themeHelper(["#0c356a", "#0174be", "#ffc436", "#fff0ce"])
);
export const blueTheme = createTheme(
  themeHelper(["#222831", "#393e46", "#00adb5", "#00fff5"])
);
